version: '3.8'

services:
  web:
    build: .
    container_name: my-app
    ports:
      - "8080:8080"
    depends_on:
      - redis
  redis:
      image: redis:latest
      container_name: my-redis
      ports:
        - "6379:6379"